---
version: 2
updates:
  # Check for updates to GitHub Actions every week
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    # Cooldown period to prevent too many PRs in a short time
    open-pull-requests-limit: 5

